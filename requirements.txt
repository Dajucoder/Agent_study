# LangChain 学习项目依赖清单
# 安装（推荐锁定版本，保证可复现）：
#     pip install -r requirements.lock
# 安装（最新兼容版本，跨环境可能略有差异）：
#     pip install -r requirements.txt
#
# Python 3.10+；langchain 1.x 时代（langchain-classic 提供 0.x 的 Agent/Memory API）。

# ---- 核心框架 ----
langchain>=1.0
langchain-classic>=1.0
langchain-openai>=1.0
langchain-community>=0.3

# ---- 提示词与输出解析 ----
langchain-core>=1.0
pydantic>=2.13.4

# ---- 文档切分与文本处理 ----
langchain-text-splitters>=1.0

# ---- 向量库（入门推荐 Chroma）----
chromadb>=1.5.9

# ---- 图编排（运行 examples/03_memory_graph.py 需要）----
langgraph>=1.2.9

# ---- 嵌入与 OpenAI 客户端 ----
openai>=1.30
tiktoken>=0.7

# ---- 环境变量管理 ----
python-dotenv>=1.2.2

# ---- 配置管理（P41：pydantic-settings 集中管理 .env）----
pydantic-settings>=2.14.2

# ---- 可观测性（可选，LangSmith）----
# 安装：pip install langsmith
# 在 .env 中设置 LANGCHAIN_TRACING_V2=true 即可启用

# ---- 服务化部署（运行 examples/06_langserve.py 需要）----
langserve>=0.3.3
fastapi>=0.139.0
uvicorn>=0.51.0

# ---- 交互环境 ----
jupyter>=1.1.1
notebook>=7.6.0

# ---- 测试（可选）----
# pip install pytest ruff

# ---- 文档站（构建 mkdocs 站点，可选；CI 在 .github/workflows/mkdocs.yml 中安装）----
# mkdocs mkdocs-material mkdocs-static-i18n
# 注：mkdocs-static-i18n 负责把 docs/*.en.md 渲染为 /en/ 双语子站点（P40）
